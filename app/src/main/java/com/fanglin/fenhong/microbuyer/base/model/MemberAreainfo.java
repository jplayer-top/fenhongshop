package com.fanglin.fenhong.microbuyer.base.model;

/**
 * 作者： Created by Plucky on 15-10-3.
 */

public class MemberAreainfo {
    public String provinceid;// 省份id (可选，必须同时传，否则不更新)
    public String cityid;// 地级市id (可选，必须同时传，否则不更新)
    public String areaid;// 区县id (可选，必须同时传，否则不更新)
    public String area_info;// 所在地信息 (可选，必须同时传，否则不更新)
}